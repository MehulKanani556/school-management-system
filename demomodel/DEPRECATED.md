# Deprecated — not used by the application

This folder contains an alternate **tenant-based** schema design. The running app uses `back/models/` with `schoolId` isolation.

Do not import these models in production code. Safe to delete after confirming no external scripts depend on them.
